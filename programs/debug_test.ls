let () = println "Step 1: Starting"

type Pair<a> = (a, a)

let () = println "Step 2: Defined Pair type"

let int_pair = (5, 10)

let () = println "Step 3: Created pair without annotation"
let () = println (int_to_str 42)
