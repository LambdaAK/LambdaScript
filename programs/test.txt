type token_type =
  | TNum of int
  | TPlus
  | TMinus
  | TTimes
  | TDiv

type Option<a> =
  | None
  | Some of a

type Parser<a> = [token_type] -> Option<(a, [token_type])>

let (<|>) p1 p2 =
  \tokens ->
    switch p1 tokens =>
    | Some result -> Some result
    | None -> p2 tokens

