type rec List =
  | Nil
  | Cons of (int, List)
  | ConsTwo of (int, List, List)

let rec big_list = 
  let rec make_list n acc =
    if n == 0 then acc
    else make_list (n - 1) (Cons (n, acc))
  in make_list 10000 Nil
