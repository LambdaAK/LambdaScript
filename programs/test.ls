let () = println (compare 1 2)
let () = println (compare 3 3)
let () = println (compare 5 1)

let () = println (1 < 2)
let () = println (2 < 1)
let () = println (3 <= 3)
let () = println (3 > 2)
let () = println (1 >= 2)

let () = println (compare "apple" "banana")
let () = println (compare "z" "a")
let () = println ("hello" < "world")

let () = println (compare 'a' 'z')
let () = println ('b' > 'a')
