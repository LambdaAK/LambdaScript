type Point = {
  x: int,
  y: int
}

type Person = {
  name: str,
  age: int,
  active: bool
}

let p1 : Point = {x: 10, y: 20}
let p2 = {x: 5, y: 15}

let x_coord = p1.x
let y_coord = p1.y

let distance = fn (p : Point) -> p.x * p.x + p.y * p.y

let _ = distance p1
